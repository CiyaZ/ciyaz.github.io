搜狗拼音Linux版使用的目录文件夹：

```
~/.sogouinput 意义不明，可能是个人词库
~/.config/sogou-qimpanel 用户安装的皮肤
~/.config/SogouPY 一套二进制文件，可能是对应用户的
~/.config/SogouPy.users 可能是用户的个人配置
/usr/share/fcitx-sogoupinyin 程又一套二进制文件，应该是程序本体
/usr/share/sogoupinyin 两个毫无意义的配置文件
/usr/share/sogoupinyin-qimpanel 自带皮肤，图标等，实际上并不会被加载，不知道有什么用
```
